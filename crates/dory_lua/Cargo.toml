[package]
name = "dory_lua"
version.workspace = true
edition.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
dory_core = { path = "../dory_core" }
mlua = { workspace = true, features = ["lua54", "send", "vendored"] }
log.workspace = true

[dev-dependencies]
uuid.workspace = true
tempfile = "3"
